# SatQuery EO-VLM service (CPU-run real VLM path)
# ==============================================
# This installs the transformers-based CPU backend. It runs a REAL
# vision-language model (default: Qwen/Qwen2-VL-2B-Instruct) genuinely on CPU.
#
# For the real TEOChat checkpoint (jirvin16/TEOChat / CUDA), install the
# OFFICIAL TEOChat repo into its own conda env INSTEAD (see README.md):
#   git clone https://github.com/ermongroup/TEOChat
#   pip install -e .            # pins its own older torch stack
# SatQuery talks to both over HTTP, so the environments never collide.

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
requests>=2.31
Pillow>=10.2
numpy>=1.26
opencv-python-headless>=4.9
rasterio>=1.3.9
huggingface_hub>=0.24
psutil>=5.9
transformers>=4.49        # Qwen2-VL support; 5.x works too
torch>=2.2                # CPU wheel: pip install torch --index-url https://download.pytorch.org/whl/cpu
safetensors>=0.4.3
accelerate>=0.30; python_version >= "3.9"   # optional, enables device_map on GPU hosts